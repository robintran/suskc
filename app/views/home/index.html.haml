.left-menu
  .background
    .title
      %i.icon-list-ul.title-icon
      %span.title-copy Categories
    .inside
      .category
        = render partial: 'category_item', locals: {cat_name: "Digital Company", icon_class: "icon-save"}
      .category
        = render partial: 'category_item', locals: {cat_name: "Investor", icon_class: "icon-money"}
      .category
        = render partial: 'category_item', locals: {cat_name: "Coworking Space", icon_class: "icon-print"}
      .category
        = render partial: 'category_item', locals: {cat_name: "Accelerator", icon_class: "icon-dashboard"}
      .category
        = render partial: 'category_item', locals: {cat_name: "Freelancer", icon_class: "icon-pencil"}
      .category
        = render partial: 'category_item', locals: {cat_name: "Startup", icon_class: "icon-fire"}
      .category
        = render partial: 'category_item', locals: {cat_name: "Hiring", icon_class: "icon-briefcase"}

#company.left-menu.full.slide-out.hidden
  .background
    .title
      = link_to "<i class='icon-remove-sign title-close-icon'></i>".html_safe, "close"
    .inside.inside-full
      .single-company
        #company-image
          = image_tag 'think-big.png'
        .single-company-info
          %i.icon-map-marker.left-location-icon
          %span#company_name.title Think Big Partners
          .info.location
            .address Kansas
            .city Kansas
          .info-item
            %i.icon-phone.company-info-icon.phone-icon
            %span.phone
          .info-item
            %i.icon-truck.company-info-icon.email-icon
            %span.email
          .info-item
            %i.icon-globe.company-info-icon
            %span.url startupshopkc.com
          .info-item
            %i.icon-twitter.company-info-icon.twitter-iconn
            %span.twitter @suskc
          .info-item
            %i.icon-facebook-sign.company-info-icon
            %span.facebook /suskc
        .company-description
          Think Big Partners is an early-stage business incubator, startup accelerator and mentorship-based collaborative network that takes ideas, opportunities and entrepreneurs and makes them big.

#category.left-menu.full.hidden
  .background
    .title
      %i.icon-save.title-icon
      %span.title-copy Digital Company
      = link_to "<i class='icon-remove-sign title-close-icon'></i>".html_safe, "close"
    .inside.inside-full
      #company
      .back
        = link_to "<i class='icon-arrow-left back-icon'></i>".html_safe, "close"
        %span= link_to 'back', 'close'

- if params[:action] == 'search'
  #search_result.left-menu.full       
    .background
      .title
        %span.title-copy Search Result
        = link_to "<i class='icon-remove-sign title-close-icon'></i>".html_safe, "close"
      .inside.inside-full
        - @locations.each do |loc|
          - logo = loc.logo.blank? ? "/assets/think-big.png" : loc.logo  
          - data = {"pos"=> {"lat"=> loc.latitude, "lng"=> loc.longitude}, "name"=> "#{loc.name}", "email"=> "#{loc.email}", "address"=> "#{loc.address}", "id"=> "#{loc.id}", "url"=> "#{loc.url}", "twitter"=> "#{loc.twitter}", "facebook"=> "#{loc.facebook}", "description"=> "#{loc.description}", "logo"=> "#{logo}"}.to_json
          .category.company
            = link_to "<i class='icon-map-marker'></i>".html_safe, '#', data: data, class: 'location_item'
            %span.title=link_to loc.name, '#', data: data, class: 'location_item'
            = link_to "<i class='icon-arrow-right arrow-icon'></i>".html_safe, '#', data: data, class: 'location_item'
        .back
          = link_to "<i class='icon-arrow-left back-icon'></i>".html_safe, "close"
          %span=link_to 'back', 'close'
#add_something 
  = render partial: 'add_something'
            
:javascript
  $("a[href=add_something]").click(function(e){
    $("#popover").removeClass('hidden');
    e.preventDefault();
  });
  
  $("a[href=login_popup]").click(function(e){
    $("#popover").removeClass('hidden');
    e.preventDefault();
  });
  
  $(".loc_info .close a").click(function(e){
    $(".loc_info").addClass('hidden');
    e.preventDefault();
  });
  
  $("#category a[href=close]").click(function(e){
    $("#category").addClass('hidden');
    $("#company").addClass('hidden');
    e.preventDefault();
  });
  
  $("#search_result a[href=close]").click(function(e){
    $("#search_result").addClass('hidden');
    $("#company").addClass('hidden');
    e.preventDefault();
  });
  
  $("#company a[href=close]").click(function(e){
    $("#company").addClass('hidden');
    e.preventDefault();
  });
    
  function getIconClass(category){
    icon_class = 'icon-save';
    if(category=='Digital Company'){
      icon_class = 'icon-save';
    }else if(category=='Investor'){
      icon_class = 'icon-money';
    }else if(category=='Coworking Space'){
      icon_class = 'icon-print';
    }else if(category=='Accelerator'){
      icon_class = 'icon-dashboard';
    }else if(category=='Freelancer'){
      icon_class = 'icon-pencil';
    }else if(category=='Startup'){
      icon_class = 'icon-fire';
    }else if(category=='Startup'){
      icon_class = 'icon-biefcase';
    }
    return icon_class;
  }
  
  $(".left-menu a.category_item").click(function(e){
    e.preventDefault();
    $(".location_item").removeClass('active');
    $("#fading-image").show();
    $("#mymap").gmap3({action:'clear'});
    $url = $(this).attr("href");
    category = $(this).attr("rel");
    icon_class = getIconClass(category);
    data_obj = {category: category}
    $("#category .title-copy").html(category);
    $.ajax({
      type: "GET",
      url: $url,
      data: data_obj,
      success: function(result) {
        $("#category.left-menu").removeClass('hidden');
        list = result;
        $("#category .inside #company").text('');
        for(i=0; i<list.length; i++){
          id = list[i].data.id;
          data= '{"pos": {"lat": ' + list[i].lat + ', "lng": ' + list[i].lng + '}, "name": "' + list[i].data.name + '", "email": "' + list[i].data.email + '", "address": "' + list[i].data.address + '", "id": "' + list[i].data.id + '", "url": "' + list[i].data.url + '", "twitter": "' + list[i].data.twitter + '", "facebook": "' + list[i].data.facebook + '", "description": "' + list[i].data.description+ '", "logo": "' + list[i].data.logo + '"}';
          inside_content="<div class='category company'><a href='#', data='" + data + 
          "', class='location_item'><i class='" + icon_class + 
          "'></i></a><span class='title'><a href='#', data='" + data + "', class='location_item'>" +
          list[i].data.name + "</a></span><a href='#', data='" + data + 
          "', class='location_item'><i class='icon-arrow-right arrow-icon'></i></a><div class='info'><div class='address'>" + 
          list[i].data.address + "</div><div class='city'>" + list[i].data.email + "</div></div></div>";

          $("#category .inside #company").append(inside_content);
        }
        addMarkers(list);
        $("#fading-image").hide();
      }
    });
  });     

  $(".location_item").live('click', function(){
    $(".location_item").removeClass('active');
    $(this).addClass('active');
    data = JSON.parse($(this).attr("data"));
    pos = data.pos;
    latLng = new google.maps.LatLng(pos.lat, pos.lng);
    content = "<div class='marker_info'><div class='right close'><a href='#'>x</a></div><div class='info'><h3>" + data.name + "</h3>" + data.email + "<br/>" + data.address + "</div></div>";
    make_overlay(latLng, content, data.id);
    load_company_menu(data);       
    $("#mymap").gmap3({action: 'setCenter', args:[latLng]});
    $("#mymap").gmap3('get').setZoom(16);
  })
  
  $(".marker_info .close a").live('click', function(e){
    $("#mymap").gmap3({action: 'clear', name: 'overlay'});
  });

  var cluster;  
  $(document).ready(function(){
    sreen_w=screen.width - 420;
    $("#mymap").css('width', sreen_w);
    $("#map_wrapper").css('width', sreen_w);
    $("#mymap").gmap3({ action: 'init',
      options:{
        center:[39.0997265, -94.5785667],
        zoom: 12,
        minZoom: 10
      },
      callback: function(map){
        waitForBounds(map);
      },
      events:{
        click: function(map, event){
          //addMarkerToCluster($(this), event.latLng);
        },
        rightclick: function(map, event){
          //addMarkersToCluster($(this), event.latLng);
        }
      }
    });
  });
      
  function waitForBounds(map){
    var ne, sw, bounds = map.getBounds();
    if (!bounds){
      google.maps.event.addListenerOnce(map, 'bounds_changed', 
        function() {
          waitForBounds(map)
        }
      );
      return;
    }
    ne = bounds.getNorthEast();
    sw = bounds.getSouthWest();
    list = #{Point.get_points(@locations).to_json.html_safe}
    addMarkers(list);
  }
  
  function addMarkers(list){
    $('#mymap').gmap3(
      { action: 'addMarkers',
        radius: 100,
        markers: list,
        clusters:{
      	  // This style will be used for clusters with more than 0 markers
      		0: {
       		  content: '<div class="cluster cluster-1">CLUSTER_COUNT</div>',
       			width: 53,
       			height: 52
       		},
       	  // This style will be used for clusters with more than 20 markers
        	20: {
        	  content: '<div class="cluster cluster-2">CLUSTER_COUNT</div>',
        		width: 56,
        		height: 55
        	},
          // This style will be used for clusters with more than 50 markers
        	50: {
            content: '<div class="cluster cluster-3">CLUSTER_COUNT</div>',
         	  width: 66,
          	height: 65
          }
        },
        callback: function(ref){ // get the cluster reference and save it in global variable
          cluster = ref;
        },
        cluster:{
          events:{
            click: function(cluster, event, data) {
              points = getPoints(data.latLng, list)
              if(points.length > 1){
                companies=""
                for(i=0; i< points.length; i++){
                
                  id = points[i].data.id;
                  pdata= '{"pos": {"lat": ' + points[i].lat + ', "lng": ' + points[i].lng + '}, "name": "' + 
                  points[i].data.name + '", "email": "' + points[i].data.email + '", "address": "' + 
                  points[i].data.address + '", "id": "' + points[i].data.id + '", "url": "' + 
                  points[i].data.url + '", "twitter": "' + points[i].data.twitter + '", "facebook": "' + 
                  points[i].data.facebook + '", "description": "' + points[i].data.description + 
                  '", "logo": "' + points[i].data.logo + '"}';
                  
                  item= "<a href='#', data='" + pdata + "', class='location_item'>" + 
                  points[i].data.name + "</a>";                
                
                  companies = companies + "<li>" + item + "</li>";
                }
                content = "<div class='marker_info'><div class='right close'><a href='#'>x</a></div><div class='info'><h3>" + points.length + " Companies here: </h3>"+ companies + "</div></div>";
                make_overlay(data.latLng, content, '');
              }
              cluster.map.panTo(data.latLng);
              cluster.map.setZoom(cluster.map.getZoom() + 1);
            },
            mouseover: function(cluster, event, data){
              //$(cluster.getDOMElement()).css('border', '1px solid #FF0000');
            },
            mouseout: function(cluster, event, data){
              //$(cluster.getDOMElement()).css('border', '0px');
            }
       	  }
        },
        marker: {
          options: {
            icon: "../assets/bg-digital.png"
          },
          events:{  
            click: function(marker, event, data){
              pos = marker.getPosition();
              content = "<div class='marker_info'><div class='right close'><a href='#'>x</a></div><div class='info'><h3>" + data.name + "</h3>" + data.email + "<br/>" + data.address + "</div></div>";
              make_overlay(pos, content, data.id);
              load_company_menu(data);         
              marker.map.panTo(pos);
              marker.map.setZoom(16);
            },
            mouseout: function(){
              //$(this).gmap3({action:'clear', name:'overlay'});
            }
          }
        }
      }
    );
  }
  
  function make_overlay(pos, content, id){
    $(".loc_info").addClass('hidden');
    $(".location_item").removeClass('active');
    if(id != ''){
      $("li#"+id+" a").addClass('active');
    }
    $("#mymap").gmap3({action:'clear', name:'overlay'});
    $("#mymap").gmap3(
      { action:'clear', name:'overlay'},
      { action:'addOverlay',
        latLng: pos,
        content: content,
        offset: {
          x: 10,
          y: -10
        }
      }
    );  
  }
  
  function load_company_menu(data){
    $("#company-image img").attr('src', data.logo)
    $("#company #company_name").html(data.name);
    $("#company .address").html(data.address);
    $("#company .city").html("Kansas");
    $("#company span.phone").html(data.phone);
    $("#company span.email").html(data.email);
    $("#company span.url").html(data.url);
    $("#company span.twitter").html(data.twitter);
    $("#company span.facebook").html(data.facebook);
    $("#company .company-description").html(data.description);
    $("#company.left-menu").removeClass('hidden');
  }

  function getPoints(pos, list){
    points = []
    for (i = 0; i < list.length; i++){
      if(list[i].lat.toFixed(7) == pos.lat().toFixed(7) && list[i].lng.toFixed(7) == pos.lng().toFixed(7)){
        points.push(list[i]);
      }
    }
    return points;
  }
